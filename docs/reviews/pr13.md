# PR13 review follow-up

Added masked MHA/GQA comparisons against independent dense attention for head dimensions 7/32/64/128/256 in FP32/FP16/BF16, including poisoned inactive rows, finite positive-zero output and unchanged full KV storage. Compact dense comparisons now also cover dimension 256. The original recorded model matrix remains Ouro MHA/dimension128; it is not rewritten to claim the expanded kernel coverage.

The kernel entry point now explicitly requires positive active context lengths. KV scatter documentation explains stride-specialized compilation; the padded Torch path is identified as a diagnostic path with per-layer indexing overhead. No kernel arithmetic, layout or default execution behavior changed.

Pre-device check: 13 CPU tests passed and 49 reserved-device tests skipped, with CUDA blocked; Ruff passed. The review device check is one invocation of the two kernel test files on one exact scheduler-reserved GPU, bounded to 10 minutes, with no model download or performance claim. Its command, execution source and actual result will be recorded after completion. No failed historical measurement is replaced.

Device result: **62 passed in 16.27s** on reserved L20X GPU2 at clean source `9ab45792bc327a2975385ee7b9c05525f1a5c059`; one invocation, final task-owned CUDA allocated/reserved bytes 0/0. This includes all 49 device cases and the 13 CPU cases. [Raw log](evidence/pr13-gpu.log) and [source/device/cleanup record](evidence/pr13-gpu-result.json) are retained. Command: `gpu run --gpu-ids 2 --wait 5m --timeout 10m --note 'vllm-lt PR13 review masked head256 correctness 9ab4579' -- numactl --physcpubind=0-7 --membind=0 env OMP_NUM_THREADS=1 PYTHONPATH=/home/hsliu2/tmp/vllm-lt-m3-inactive /home/hsliu2/tmp/venvs/vllm-lt-q1/bin/python /home/hsliu2/tmp/vllm-lt-review-fixes-20260911/run_pr13_gpu.py`. The driver runs `pytest -q tests/test_attention.py tests/test_inactive_kernels.py --run-gpu` and records task-owned cleanup; its SHA256 is in the JSON.
