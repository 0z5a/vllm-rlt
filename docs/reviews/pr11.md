# PR11 review follow-up

Oracle cleanup now records retained storage/weight ownership before and after close, including exceptional cleanup, and rejects a close that retains state. Native snapshot documentation identifies its private allocation dependencies as a cache-refactor tripwire. The numerical contract explicitly retains mandatory pinned-official checks and confirms deliberate, predeclared FP32 tolerances.

Validation: 59 targeted runner/oracle CPU tests passed with CUDA blocked, including normal, no-op and raising oracle cleanup. Ruff passed. These observer/cleanup changes do not change inference arithmetic; the published numerical evidence remains bound to its original source and is not relabeled as a new run.

The requested same-state versus accumulated-state BF16 diagnosis is specified in `docs/q1-validation.md` as a successor diagnostic requirement. It has not been executed in this review-fix pass. BF16 and AC-Q1-05 remain unresolved; no tolerance, threshold or discrete agreement exemption changed.
