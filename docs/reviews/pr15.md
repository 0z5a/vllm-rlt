# PR15 review follow-up

Fixed both P2 findings: guard Python 3.11 exception-note APIs on Python 3.10-compatible failure paths, and retain later graph settlement failures as secondary serialized events without retrying device completion or retaining traceback-owned tensors. The same note guard covers graph benchmark/validation cleanup paths. The parent metadata-specification change is integrated while retaining graph-specific four/eight-row validation.

Validation: **157 targeted graph, budget-decline, pool-ownership, transaction, harness, lifecycle and persistent-buffer CPU tests passed**, with CUDA blocked; Ruff passed. Tests exercise both budget-refusal paths with `add_note` absent and repeated failure reporting for confirmed/unconfirmed completion. This simulated compatibility check runs under the prepared Python 3.12 environment; it is not a native Python 3.10 interpreter run.

No healthy traversal math, capture bucket policy or kernel changed in this fix. Existing GPU replay/performance evidence remains bound to its original source; the PR stays draft. `docs/m3-cuda-graphs.md` records the requested CPU profile-coverage proof, separate compact deployment baseline and executor-lifetime plateau as requirements for a new qualification contract. Those new experiments have not run in this review pass.
