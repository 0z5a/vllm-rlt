# PR12 review follow-up

The NUMA test now installs its simulated Linux affinity function even on platforms where `os.sched_getaffinity` does not exist. Validation: 21 targeted A/B-schema CPU tests passed with CUDA blocked; Ruff passed. The actual GPU benchmark remains Linux/scheduler-specific.

The model/cache call site and standalone write adapter now spell out the traversal contract, validation ordering and full-descriptor cost. The private descriptor is intentionally an internal ownership lease shared by model/cache/runner, not a stable user API. A public facade and a cheaper standalone-write path are deferred API/performance changes: no production hot-path caller uses that standalone adapter, and adding aliases would not remove its lifecycle obligations. The original M2 measured code and 20–21% W1 evidence remain unchanged by these comments/test changes.

## Precision requirements revision — 2026-09-12

The [precision policy](../precision-policy.md) makes BF16 the primary GPU
inference/validation/performance target, with FP32 accumulation and sensitive
intermediates specified per operation. README examples, roadmap, and Q1/M1/M2
guides distinguish new requirements from historical contracts. Exact cache and
metadata-only A/B invariants remain mandatory; differing arithmetic paths use
justified BF16 numerical/decision criteria. BF16 data collection can proceed
alongside historical reference diagnosis. Runtime defaults, kernels, executable
schemas, frozen JSON contracts, and original acceptance results are unchanged;
BF16 harness implementation and measured comparisons remain follow-up work.

Validation: documentation-only diff, balanced code fences, relative Markdown
links/anchors, and `git diff --check` passed. No new device experiment was run.

## Original Ouro source audit — 2026-09-12

The [paper/source notes](../paper-notes.md) now distinguish the original Ouro
paper from CDB and record the published BF16 configuration and explicit FP32
boundaries. The official gate path has no explicit FP32 promotion; the current
runner's probability precision and paged-attention intermediates are labeled
project differences. Requirements use author guidance first and call for the
paper's 3-shot CoT GSM8K protocol in a new BF16 accuracy comparison. The old
4/64 FP32 screen and all numerical failures remain recorded. Source bytes were
verified against the vendored official model. This audit runs no new inference.
Validation: four Markdown files, 13 relative links/anchors, code fences, the
paper's evaluation-table anchor, and `git diff --check` passed.
