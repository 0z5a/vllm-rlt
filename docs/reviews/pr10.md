# PR10 review follow-up

- Label the profiler's automatic recommendation as a kernel-time ranking, not the independently correlated M2 nomination.
- Clarify at CUDA event recording that the stream span includes host submission gaps.
- Reject a missing final synchronization boundary with the collector's explicit diagnostic, and exclude that failed run from comparisons.

Validation: 28 targeted benchmark report/runner CPU tests passed with CUDA discovery and initialization blocked; Ruff and formatting passed. The regression test checks the error and pair exclusion. The engine and measured path are unchanged except for a comment; no new device run or performance claim is needed. Published M1 measurements retain their original source identity and bytes.
